{
  if (isDisposed() || position.isDisposed())   return;
  String selText=CoreMessages.editor_binary_hex_status_line_selection + sel[0] + " (0x"+ Long.toHexString(sel[0])+ ") - "+ sel[1]+ " (0x"+ Long.toHexString(sel[1])+ ")";
  position.setText(selText);
}
