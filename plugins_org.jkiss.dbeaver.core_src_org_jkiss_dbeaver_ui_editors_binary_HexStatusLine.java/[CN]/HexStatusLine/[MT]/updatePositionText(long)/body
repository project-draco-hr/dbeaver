{
  if (isDisposed() || position.isDisposed())   return;
  String posText=CoreMessages.editor_binary_hex_status_line_offset + pos + " (dec) = "+ Long.toHexString(pos)+ " (binary)";
  position.setText(posText);
}
