{
  Object o=infoMap.get(type);
  if (o == null) {
    try {
      o=type.newInstance();
    }
 catch (    Exception e) {
      log.error("Can't create context info " + type.getName(),e);
    }
    infoMap.put(type,o);
  }
  return type.cast(o);
}
