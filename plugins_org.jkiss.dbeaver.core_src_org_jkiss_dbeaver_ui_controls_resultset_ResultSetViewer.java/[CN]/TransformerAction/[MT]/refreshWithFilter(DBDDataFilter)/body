{
  DBSDataContainer dataContainer=getDataContainer();
  if (dataContainer != null) {
    runDataPump(dataContainer,filter,0,getSegmentMaxRows(),curRow == null ? -1 : curRow.getRowNumber(),null);
  }
}
