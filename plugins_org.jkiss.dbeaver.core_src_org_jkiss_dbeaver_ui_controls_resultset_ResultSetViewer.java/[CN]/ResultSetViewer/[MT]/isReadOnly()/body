{
  if (model.isUpdateInProgress() || !(activePresentation instanceof IResultSetEditor)) {
    return true;
  }
  DBPDataSource dataSource=getDataSource();
  return dataSource == null || !dataSource.isConnected() || dataSource.getContainer().isConnectionReadOnly() || dataSource.getInfo().isReadOnlyData();
}
