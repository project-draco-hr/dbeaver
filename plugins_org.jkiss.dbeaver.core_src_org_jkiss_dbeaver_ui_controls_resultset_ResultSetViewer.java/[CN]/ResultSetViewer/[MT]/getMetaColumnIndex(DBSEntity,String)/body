{
  for (int i=0; i < visibleColumns.length; i++) {
    DBDAttributeBinding column=visibleColumns[i];
    if ((table == null || column.getRowIdentifier().getEntity() == table) && column.getAttributeName().equals(columnName)) {
      return i;
    }
  }
  return -1;
}
