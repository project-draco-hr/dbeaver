{
  for (int i=0; i < metaColumns.length; i++) {
    DBDAttributeBinding column=metaColumns[i];
    if ((table == null || column.getValueLocator().getEntity() == table) && column.getColumnName().equals(columnName)) {
      return i;
    }
  }
  return -1;
}
