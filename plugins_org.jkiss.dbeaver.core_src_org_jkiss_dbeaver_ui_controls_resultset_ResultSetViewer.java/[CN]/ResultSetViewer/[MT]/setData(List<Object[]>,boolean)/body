{
  this.closeEditors();
  model.setData(rows,updateMetaData);
  if (updateMetaData) {
    this.initResultSet();
  }
 else {
    this.refreshSpreadsheet(updateMetaData,true);
  }
  updateEditControls();
}
