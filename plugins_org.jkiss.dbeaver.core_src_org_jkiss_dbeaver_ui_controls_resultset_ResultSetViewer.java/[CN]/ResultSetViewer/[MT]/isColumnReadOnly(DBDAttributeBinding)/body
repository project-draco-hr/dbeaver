{
  if (isReadOnly()) {
    return true;
  }
  if (!model.isColumnReadOnly(column)) {
    return false;
  }
  boolean newRow=(curRow != null && curRow.getState() == RowData.STATE_ADDED);
  return !newRow;
}
