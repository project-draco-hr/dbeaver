{
  if (isReadOnly()) {
    return true;
  }
  if (!model.isColumnReadOnly(column)) {
    return false;
  }
  boolean newRow=(curRow != null && curRow.getState() == ResultSetRow.STATE_ADDED);
  return !newRow;
}
