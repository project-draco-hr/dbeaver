{
  if (horizontal) {
    if (!recordMode) {
      return model.getVisibleColumns().toArray();
    }
 else {
      return new Object[]{curRow};
    }
  }
 else {
    if (!recordMode) {
      return model.getAllRows().toArray();
    }
 else {
      return model.getVisibleColumns().toArray();
    }
  }
}
