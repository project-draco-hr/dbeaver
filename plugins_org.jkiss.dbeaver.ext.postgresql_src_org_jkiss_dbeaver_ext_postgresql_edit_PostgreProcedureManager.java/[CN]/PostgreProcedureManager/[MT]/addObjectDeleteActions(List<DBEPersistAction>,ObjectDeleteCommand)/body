{
  String objectType=command.getObject().isAggregate() ? "AGGREGATE" : "FUNCTION";
  actions.add(new SQLDatabasePersistAction("Drop procedure","DROP " + objectType + " "+ command.getObject().getFullQualifiedSignature()));
}
