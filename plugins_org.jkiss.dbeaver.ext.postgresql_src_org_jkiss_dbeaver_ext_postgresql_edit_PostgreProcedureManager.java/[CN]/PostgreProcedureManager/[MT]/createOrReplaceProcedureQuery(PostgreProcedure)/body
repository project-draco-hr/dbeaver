{
  String objectType=procedure.isAggregate() ? "AGGREGATE" : "FUNCTION";
  return new DBEPersistAction[]{new SQLDatabasePersistAction("Drop procedure","DROP " + objectType + " IF EXISTS "+ procedure.getFullQualifiedSignature()),new SQLDatabasePersistAction("Create procedure",procedure.getBody())};
}
