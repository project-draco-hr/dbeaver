{
  if (store instanceof IPersistentPreferenceStore) {
    try {
      ((IPersistentPreferenceStore)store).save();
    }
 catch (    IOException e) {
      log.warn(e);
    }
  }
 else {
    log.debug("Can't save preference store '" + store + "' - not a persistent one");
  }
}
