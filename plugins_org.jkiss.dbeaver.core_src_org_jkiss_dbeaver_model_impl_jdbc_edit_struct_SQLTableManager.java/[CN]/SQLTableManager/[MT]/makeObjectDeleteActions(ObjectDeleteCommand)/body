{
  return new DBEPersistAction[]{new SQLDatabasePersistAction(CoreMessages.model_jdbc_drop_table,"DROP " + (command.getObject().isView() ? "VIEW" : "TABLE") + " "+ command.getObject().getFullQualifiedName())};
}
