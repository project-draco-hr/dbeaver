{
  if (instance == null && implType != null) {
    try {
      this.instance=implType.createInstance(DBDAttributeTransformer.class);
    }
 catch (    Exception e) {
      log.error("Can't instantiate attribute transformer '" + this.id + "'",e);
    }
  }
  return instance;
}
