{
  Assert.isNotNull(key);
  Assert.isTrue(values.length > 0);
  if (fValueMap != null) {
    fValueMap.put(key,values);
    if (key.equals(fKey))     fCurrentChoice=getChoices()[0];
    setResolved(true);
  }
}
