{
  logPage.startLogReader(processBuilder,process.getErrorStream());
  boolean isFiltering=removeDefiner;
  Thread job=isFiltering ? new DumpFilterJob(monitor,process.getInputStream()) : new DumpCopierJob(monitor,process.getInputStream());
  job.start();
}
