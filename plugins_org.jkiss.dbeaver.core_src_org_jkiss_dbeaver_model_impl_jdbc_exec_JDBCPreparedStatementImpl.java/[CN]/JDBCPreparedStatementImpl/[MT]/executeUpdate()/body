{
  this.beforeExecute();
  try {
    return handleExecuteResult(getOriginal().executeUpdate());
  }
 catch (  Throwable e) {
    throw super.handleExecuteError(e);
  }
 finally {
    super.afterExecute();
  }
}
