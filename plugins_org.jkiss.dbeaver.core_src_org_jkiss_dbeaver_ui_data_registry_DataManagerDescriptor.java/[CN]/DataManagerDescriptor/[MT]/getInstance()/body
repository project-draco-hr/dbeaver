{
  if (instance == null && implType != null) {
    try {
      this.instance=implType.createInstance(IValueManager.class);
    }
 catch (    Exception e) {
      log.error("Can't instantiate value manager '" + this.id + "'",e);
    }
  }
  return instance;
}
