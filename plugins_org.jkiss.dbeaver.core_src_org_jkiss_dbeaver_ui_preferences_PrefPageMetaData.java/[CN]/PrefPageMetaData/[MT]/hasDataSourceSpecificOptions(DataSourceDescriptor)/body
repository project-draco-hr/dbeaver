{
  AbstractPreferenceStore store=dataSourceDescriptor.getPreferenceStore();
  return store.contains(PrefConstants.READ_EXPENSIVE_PROPERTIES) || store.contains(PrefConstants.META_SEPARATE_CONNECTION) || store.contains(PrefConstants.META_CASE_SENSITIVE);
}
