{
  if (isEmpty(array))   return false;
  for (  OBJECT_TYPE v : array) {
    for (    OBJECT_TYPE v2 : values) {
      if (CommonUtils.equalObjects(v,v2))       return true;
    }
  }
  return false;
}
