{
  try {
    String content=document.get(replacementPosition.getOffset(),offset - replacementPosition.getOffset());
    if (replacementString.startsWith(content))     return true;
  }
 catch (  BadLocationException e) {
  }
  return false;
}
