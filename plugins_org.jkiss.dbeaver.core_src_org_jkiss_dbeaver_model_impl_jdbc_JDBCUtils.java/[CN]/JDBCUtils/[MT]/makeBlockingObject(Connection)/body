{
  return new DBRBlockingObject(){
    @Override public void cancelBlock() throws DBException {
      try {
        connection.close();
      }
 catch (      SQLException e) {
        throw new DBCException("Can't close connection",e);
      }
    }
  }
;
}
