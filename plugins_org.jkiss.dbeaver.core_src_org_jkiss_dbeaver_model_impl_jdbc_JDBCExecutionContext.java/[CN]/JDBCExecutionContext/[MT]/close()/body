{
synchronized (this) {
    if (connection != null) {
      try {
        connection.close();
      }
 catch (      Throwable ex) {
        log.error(ex);
      }
      connection=null;
    }
    super.closeContext();
  }
  this.dataSource.allContexts.remove(this);
}
