{
synchronized (this) {
    if (connection != null) {
      try {
        connection.close();
      }
 catch (      Throwable ex) {
        log.error(ex);
      }
      QMUtils.getDefaultHandler().handleContextClose(this);
      connection=null;
    }
  }
  this.dataSource.allContexts.remove(this);
}
