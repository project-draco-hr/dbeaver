{
  StringBuilder sbResult=new StringBuilder();
  for (  String o : value) {
    sbResult.append(o.trim());
    if (delimiter != null && !delimiter.isEmpty())     sbResult.append(delimiter);
  }
  return sbResult.toString();
}
