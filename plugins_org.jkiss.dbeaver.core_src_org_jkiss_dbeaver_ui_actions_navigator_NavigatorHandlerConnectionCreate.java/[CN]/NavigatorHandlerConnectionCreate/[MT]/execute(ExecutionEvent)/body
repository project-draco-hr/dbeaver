{
  IWorkbenchWindow window=HandlerUtil.getActiveWorkbenchWindow(event);
  CreateConnectionDialog dialog=new CreateConnectionDialog(window,new NewConnectionWizard(DBeaverCore.getInstance().getProjectRegistry().getActiveDataSourceRegistry()));
  dialog.open();
  return null;
}
