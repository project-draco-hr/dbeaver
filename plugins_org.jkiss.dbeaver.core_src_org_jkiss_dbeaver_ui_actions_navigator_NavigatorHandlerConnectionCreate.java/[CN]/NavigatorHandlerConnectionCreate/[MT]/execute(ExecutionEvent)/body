{
  IWorkbenchWindow window=HandlerUtil.getActiveWorkbenchWindow(event);
  ConnectionDialog dialog=new ConnectionDialog(window,new NewConnectionWizard(DBeaverCore.getInstance().getProjectRegistry().getActiveDataSourceRegistry()));
  dialog.open();
  return null;
}
