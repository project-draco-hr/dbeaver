{
  if (name == null) {
    return false;
  }
  boolean validName=false;
  for (int i=0; i < name.length(); i++) {
    if (Character.isLetter(name.charAt(i))) {
      validName=true;
      break;
    }
  }
  return validName;
}
