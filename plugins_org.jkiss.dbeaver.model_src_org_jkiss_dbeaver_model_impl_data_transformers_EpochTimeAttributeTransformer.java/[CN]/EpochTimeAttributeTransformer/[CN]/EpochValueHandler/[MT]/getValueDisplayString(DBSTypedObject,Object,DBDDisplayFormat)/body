{
  if (value instanceof Number) {
    return new SimpleDateFormat("yyyy-MM-dd HH:mm",Locale.ENGLISH).format(new Date(((Number)value).longValue()));
  }
  return DBUtils.getDefaultValueDisplayString(value,format);
}
