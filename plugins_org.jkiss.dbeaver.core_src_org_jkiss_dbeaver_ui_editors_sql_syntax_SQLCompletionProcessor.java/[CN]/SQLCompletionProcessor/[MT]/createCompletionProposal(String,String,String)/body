{
  return new SQLCompletionProposal(editor.getSyntaxManager(),displayString,replaceString,wordDetector,replaceString.length(),null,new ContextInformation(null,displayString,displayString),description,null);
}
