{
  if (!result) {
    try {
      updateCount=getOriginal().getUpdateCount();
    }
 catch (    SQLException e) {
      log.warn("Could not obtain update count",e);
    }
  }
 else {
    updateCount=0;
  }
  return result;
}
