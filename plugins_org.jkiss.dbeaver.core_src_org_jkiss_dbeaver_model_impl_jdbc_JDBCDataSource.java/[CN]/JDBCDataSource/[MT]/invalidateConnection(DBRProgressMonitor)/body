{
  if (connection == null) {
    connection=openConnection(monitor);
    return;
  }
  if (!JDBCUtils.isConnectionAlive(connection.getConnection())) {
    close();
    connection=openConnection(monitor);
  }
}
