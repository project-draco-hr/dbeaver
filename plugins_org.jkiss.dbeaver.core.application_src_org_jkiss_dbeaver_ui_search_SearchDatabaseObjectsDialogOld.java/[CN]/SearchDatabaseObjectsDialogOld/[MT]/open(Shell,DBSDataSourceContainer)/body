{
  if (instance != null) {
    instance.getShell().setActive();
    return;
  }
  SearchDatabaseObjectsDialogOld dialog=new SearchDatabaseObjectsDialogOld(shell,currentDataSource);
  instance=dialog;
  try {
    dialog.open();
  }
  finally {
    instance=null;
  }
}
