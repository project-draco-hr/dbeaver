{
  String value=p != null ? p.get(name) : null;
  if (value == null) {
    return parentStore != null && useParent ? (useParentDefault ? parentStore.getDefaultString(name) : parentStore.getString(name)) : STRING_DEFAULT_DEFAULT;
  }
  return value;
}
