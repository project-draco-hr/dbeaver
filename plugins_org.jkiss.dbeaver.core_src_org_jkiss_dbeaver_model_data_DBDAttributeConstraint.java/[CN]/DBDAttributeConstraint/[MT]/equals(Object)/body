{
  if (!(obj instanceof DBDAttributeConstraint)) {
    return false;
  }
  DBDAttributeConstraint source=(DBDAttributeConstraint)obj;
  return equalFilters(source) && this.visible == source.visible && this.visualPosition == source.visualPosition;
}
