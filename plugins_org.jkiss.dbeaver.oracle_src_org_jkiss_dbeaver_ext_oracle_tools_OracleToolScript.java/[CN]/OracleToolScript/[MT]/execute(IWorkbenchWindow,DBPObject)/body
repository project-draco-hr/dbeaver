{
  if (object instanceof DBSObject && ((DBSObject)object).getDataSource() instanceof OracleDataSource) {
    ToolWizardDialog dialog=new ToolWizardDialog(window,new OracleScriptExecuteWizard((OracleDataSource)((DBSObject)object).getDataSource()));
    dialog.open();
  }
}
