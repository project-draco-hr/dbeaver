{
  long curTime=System.currentTimeMillis();
  long random=secureRand.nextLong();
  if (random < 0) {
    random=-random;
  }
  curTime^=random;
  curTime^=1249210983;
  random^=1701729106;
  return Long.toString(curTime,Character.MAX_RADIX) + Long.toString(random,Character.MAX_RADIX);
}
