{
  String name=curJob == null ? null : curJob.getLastQuery() == null ? null : CommonUtils.truncateString(curJob.getLastQuery().getQuery(),200);
  if (name == null) {
    name="SQL";
  }
  return name;
}
