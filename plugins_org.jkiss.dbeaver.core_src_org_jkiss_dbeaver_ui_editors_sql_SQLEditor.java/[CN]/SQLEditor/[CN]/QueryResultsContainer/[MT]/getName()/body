{
  final SQLQueryJob job=queryProcessor.curJob;
  String name=job == null ? null : job.getLastQuery() == null ? null : job.getLastQuery().getQuery();
  if (name == null) {
    name="SQL";
  }
  return name;
}
