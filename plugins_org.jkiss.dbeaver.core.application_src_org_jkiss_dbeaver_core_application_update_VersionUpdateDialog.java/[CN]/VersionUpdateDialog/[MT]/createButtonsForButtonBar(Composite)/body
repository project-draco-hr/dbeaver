{
  if (newVersion != null) {
    createButton(parent,INFO_ID,IDialogConstants.PROCEED_LABEL,true);
  }
  createButton(parent,IDialogConstants.CLOSE_ID,IDialogConstants.CLOSE_LABEL,newVersion == null);
}
