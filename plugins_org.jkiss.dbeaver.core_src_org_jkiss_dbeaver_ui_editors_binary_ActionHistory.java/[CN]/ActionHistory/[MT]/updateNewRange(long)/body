{
  if (newRangePosition < 0L) {
    newRangePosition=position;
    newRangeLength=1L;
  }
 else {
    if (newRangePosition > position) {
      newRangePosition=position;
    }
    ++newRangeLength;
  }
}
