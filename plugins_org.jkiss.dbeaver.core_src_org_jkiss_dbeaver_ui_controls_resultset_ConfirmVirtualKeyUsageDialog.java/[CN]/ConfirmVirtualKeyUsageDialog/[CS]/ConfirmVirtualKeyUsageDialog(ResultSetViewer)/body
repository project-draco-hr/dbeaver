{
  super(viewer.getControl().getShell(),"Confirm usage of virtual key",null,"You are about to persist changes. Virtual (set by user) unique key is used for the table '" + viewer.getVirtualEntityIdentifier().getReferrer().getParentObject().getName() + "'.\nIncorrect virtual key configuration may cause table data corruption.\nAre you sure you want to proceed?",WARNING,new String[]{IDialogConstants.OK_LABEL,IDialogConstants.CANCEL_LABEL,"Edit Virtual Key"},0,"Do not ask again for '" + viewer.getVirtualEntityIdentifier().getReferrer().getParentObject().getName() + "'",false);
  this.viewer=viewer;
}
