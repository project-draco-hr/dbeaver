{
  if (message instanceof Throwable) {
    warn(message.toString(),(Throwable)message);
    return;
  }
  DBeaverCore.getInstance().getPluginLog().log(new Status(Status.WARNING,DBeaverCore.getInstance().getPluginID(),message == null ? null : message.toString()));
}
