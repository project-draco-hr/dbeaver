{
  if (message instanceof Throwable) {
    error(message.toString(),(Throwable)message);
    return;
  }
  DBeaverCore.getInstance().getPluginLog().log(new Status(Status.ERROR,DBeaverCore.getInstance().getPluginID(),message == null ? null : message.toString()));
}
