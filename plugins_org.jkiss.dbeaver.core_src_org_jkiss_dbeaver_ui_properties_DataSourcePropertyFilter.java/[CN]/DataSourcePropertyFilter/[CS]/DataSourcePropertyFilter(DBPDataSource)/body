{
  IPreferenceStore store=dataSource != null ? dataSource.getContainer().getPreferenceStore() : DBeaverCore.getGlobalPreferenceStore();
  this.showExpensive=store.getBoolean(PrefConstants.READ_EXPENSIVE_PROPERTIES);
}
