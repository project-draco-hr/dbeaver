{
  IPreferenceStore store=dataSource != null ? dataSource.getContainer().getPreferenceStore() : DBeaverCore.getInstance().getGlobalPreferenceStore();
  this.showExpensive=store.getBoolean(PrefConstants.READ_EXPENSIVE_PROPERTIES);
}
