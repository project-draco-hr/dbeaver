{
  if (clob != null) {
    try {
      clob.free();
    }
 catch (    Throwable e) {
      if (e instanceof Exception) {
        log.warn(e);
      }
 else {
        log.debug(e);
      }
    }
    clob=null;
  }
}
