{
  toolBarManager.add(new Separator());
  if (previewController != null && !previewController.isReadOnly()) {
    toolBarManager.add(saveCellAction);
  }
  toolBarManager.add(ActionUtils.makeCommandContribution(resultSet.getSite(),ResultSetCommandHandler.CMD_TOGGLE_PANELS,CommandContributionItem.STYLE_PUSH,UIIcon.REJECT));
}
