{
  if (textAreasStart + bytesPerLine * numberOfLines > content.length())   textAreasStart=content.length() - (content.length() - 1L) % bytesPerLine - 1L - bytesPerLine * numberOfLines_1;
  if (textAreasStart < 0L)   textAreasStart=0L;
}
