{
  isInserting=insert;
  int width=0;
  int height=hexText.getCaret().getSize().y;
  if (!isInserting)   width=fontCharWidth;
  hexText.getCaret().setSize(width,height);
  previewText.getCaret().setSize(width,height);
}
