{
  error=null;
  pumpStartTime=System.currentTimeMillis();
  DBRProgressMonitor proxyMonitor=new ProxyProgressMonitor(monitor){
    @Override public void beginTask(    String name,    int totalWork){
      super.beginTask(name,totalWork);
    }
    @Override public void subTask(    String name){
      progressMessage=name;
      super.subTask(name);
    }
  }
;
  PumpVisualizer visualizer=new PumpVisualizer();
  try (DBCSession session=getExecutionContext().openSession(proxyMonitor,DBCExecutionPurpose.USER,NLS.bind(CoreMessages.controls_rs_pump_job_context_name,dataContainer.getName()))){
    visualizer.schedule(PROGRESS_VISUALIZE_PERIOD * 2);
    statistics=dataContainer.readData(session,controller.getDataReceiver(),dataFilter,offset,maxRows,DBSDataContainer.FLAG_READ_PSEUDO,controller);
  }
 catch (  DBException e) {
    error=e;
  }
 finally {
    visualizer.finished=true;
  }
  return Status.OK_STATUS;
}
