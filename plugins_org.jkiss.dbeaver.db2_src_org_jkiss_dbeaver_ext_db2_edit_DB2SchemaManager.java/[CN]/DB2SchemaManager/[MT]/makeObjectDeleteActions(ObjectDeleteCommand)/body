{
  String schemaName=command.getObject().getName();
  IDatabasePersistAction action=new AbstractDatabasePersistAction("Drop schema (SQL)",String.format(SQL_DROP_SCHEMA,schemaName));
  return new IDatabasePersistAction[]{action};
}
