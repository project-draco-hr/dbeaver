{
  visualizer.completeLoading(innerResult);
  if (innerError != null) {
    log.debug(innerError);
    UIUtils.showErrorDialog(visualizer.getShell(),getName(),innerError.getMessage(),innerError);
  }
}
