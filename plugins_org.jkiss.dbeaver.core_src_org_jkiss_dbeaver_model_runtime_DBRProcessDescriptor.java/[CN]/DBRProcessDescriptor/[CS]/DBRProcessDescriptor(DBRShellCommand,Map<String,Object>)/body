{
  this.command=command;
  String commandLine=variables == null ? command.getCommand() : GeneralUtils.replaceVariables(command.getCommand(),variables);
  processBuilder=new ProcessBuilder(GeneralUtils.parseCommandLine(commandLine));
}
