{
  int rowCount=rows.size();
  List<ResultSetRow> newRows=new ArrayList<ResultSetRow>(rowCount);
  for (int i=0; i < rowCount; i++) {
    newRows.add(new ResultSetRow(curRows.size() + i,rows.get(i)));
  }
  curRows.addAll(newRows);
}
