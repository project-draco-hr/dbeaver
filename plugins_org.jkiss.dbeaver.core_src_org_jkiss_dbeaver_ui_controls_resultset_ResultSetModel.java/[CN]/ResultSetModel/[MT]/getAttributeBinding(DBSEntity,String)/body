{
  for (  DBDAttributeBinding column : visibleColumns) {
    DBDRowIdentifier rowIdentifier=column.getRowIdentifier();
    if ((table == null || (rowIdentifier != null && rowIdentifier.getEntity() == table)) && column.getName().equals(columnName)) {
      return column;
    }
  }
  return null;
}
