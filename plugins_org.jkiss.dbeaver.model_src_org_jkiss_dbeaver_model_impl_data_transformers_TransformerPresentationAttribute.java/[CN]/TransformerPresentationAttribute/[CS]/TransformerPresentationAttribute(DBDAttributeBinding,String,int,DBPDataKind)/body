{
  super(attribute.getName(),typeName,typeId,attribute.getOrdinalPosition(),attribute.getMaxLength(),attribute.getScale(),attribute.getPrecision(),attribute.isRequired(),attribute.isAutoGenerated());
  this.dataKind=dataKind;
}
