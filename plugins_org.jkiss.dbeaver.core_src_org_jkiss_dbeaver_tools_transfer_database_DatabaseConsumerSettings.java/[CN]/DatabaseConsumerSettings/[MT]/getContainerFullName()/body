{
  DBSObjectContainer container=getContainer();
  return container == null ? null : container instanceof DBPDataSource ? DBUtils.getObjectFullName(container) : DBUtils.getObjectFullName(container) + " [" + container.getDataSource().getContainer().getName()+ "]";
}
