{
  if (isEmpty(name)) {
    return null;
  }
  name=name.trim();
  if (underscoreSpaces) {
    name=name.replace(' ','_');
  }
  try {
    return Enum.valueOf(type,name);
  }
 catch (  Exception e) {
    e.printStackTrace();
    return null;
  }
}
