{
  if (isEmpty(name)) {
    return null;
  }
  if (underscoreSpaces) {
    name=name.replace(' ','_');
  }
  try {
    return (T)Enum.valueOf(type,name);
  }
 catch (  Exception e) {
    e.printStackTrace();
    return null;
  }
}
