{
  if (otherNode instanceof DBNDiagram) {
    ERDResourceHandler.createDiagram(((DBNDiagram)otherNode).getDiagram(),otherNode.getNodeName(),(IFolder)getResource(),null);
  }
 else {
    super.dropNode(otherNode);
  }
}
