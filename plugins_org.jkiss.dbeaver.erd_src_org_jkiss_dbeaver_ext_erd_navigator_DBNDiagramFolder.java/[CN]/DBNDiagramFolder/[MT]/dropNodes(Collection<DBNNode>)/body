{
  for (  DBNNode node : nodes) {
    if (node instanceof DBNDiagram) {
      ERDResourceHandler.createDiagram(((DBNDiagram)node).getDiagram(),node.getNodeName(),(IFolder)getResource(),null);
    }
  }
}
