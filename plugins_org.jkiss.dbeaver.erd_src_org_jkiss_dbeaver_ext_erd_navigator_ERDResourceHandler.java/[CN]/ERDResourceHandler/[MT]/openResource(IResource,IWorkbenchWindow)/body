{
  if (!(resource instanceof IFile)) {
    return;
  }
  ERDEditorInput erdInput=new ERDEditorInput((IFile)resource);
  window.getActivePage().openEditor(erdInput,ERDEditorStandalone.class.getName());
}
