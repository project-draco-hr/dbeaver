{
  DBSDataSourceContainer container=getDataSourceContainer();
  return queries.size() == 1 && container != null && container.getPreferenceStore().getBoolean(DBeaverPreferences.KEEP_STATEMENT_OPEN);
}
