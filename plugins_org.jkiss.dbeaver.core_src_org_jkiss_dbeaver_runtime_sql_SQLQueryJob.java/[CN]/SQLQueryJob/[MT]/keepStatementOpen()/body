{
  DBPDataSource dataSource=getDataSource();
  return queries.size() == 1 && dataSource != null && dataSource.getContainer().getPreferenceStore().getBoolean(DBeaverPreferences.KEEP_STATEMENT_OPEN);
}
