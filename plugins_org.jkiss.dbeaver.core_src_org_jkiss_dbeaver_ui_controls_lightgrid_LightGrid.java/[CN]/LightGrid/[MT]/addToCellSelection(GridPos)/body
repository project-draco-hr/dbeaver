{
  if (newCell.col < 0 || newCell.col >= columns.size())   return;
  if (newCell.row < 0 || newCell.row >= getItemCount())   return;
  if (getColumn(newCell.col).getCellSelectionEnabled()) {
    selectedCells.add(newCell);
  }
}
