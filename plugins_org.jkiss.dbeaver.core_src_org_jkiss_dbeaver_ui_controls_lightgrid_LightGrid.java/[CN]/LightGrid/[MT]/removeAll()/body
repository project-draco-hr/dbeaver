{
  checkWidget();
  deselectAll();
  focusItem=-1;
  focusColumn=null;
  topIndex=-1;
  bottomIndex=-1;
  topColumns.clear();
  columns.clear();
  columnElements=new Object[0];
  rowElements=new Object[0];
  redraw();
}
