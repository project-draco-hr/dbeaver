{
  boolean descending=(startRow < endRow);
  int iterItem=endRow;
  do {
    getCells(endRow,cells);
    if (iterItem == startRow)     break;
    if (descending) {
      iterItem--;
    }
 else {
      iterItem++;
    }
  }
 while (true);
}
