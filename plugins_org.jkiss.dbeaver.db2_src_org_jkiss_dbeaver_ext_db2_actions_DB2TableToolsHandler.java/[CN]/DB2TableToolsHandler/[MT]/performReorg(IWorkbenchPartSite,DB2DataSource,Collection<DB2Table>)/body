{
  DB2TableReorgDialog2 dialog=new DB2TableReorgDialog2(partSite,dataSource,tables.iterator().next());
  dialog.open();
  dialog.setOnSuccess(new Runnable(){
    @Override public void run(){
      UIUtils.showMessageBox(partSite.getShell(),DB2Messages.dialog_table_tools_success_title,"Reorg finished",SWT.ICON_INFORMATION);
    }
  }
);
}
