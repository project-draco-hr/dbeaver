{
  if (objectMap == null) {
    this.objectMap=new HashMap<>();
    for (    OBJECT object : objectList) {
      String name=caseSensitive ? object.getName() : object.getName().toUpperCase();
      checkDuplicateName(name,object);
      this.objectMap.put(name,object);
    }
  }
  return objectMap;
}
