{
  JDBCPreparedStatement dbStat=context.prepareStatement("SELECT * FROM " + MSSQLConstants.META_TABLE_ROUTINES + " WHERE "+ MSSQLConstants.COL_ROUTINE_SCHEMA+ "=?"+ " ORDER BY "+ MSSQLConstants.COL_ROUTINE_NAME);
  dbStat.setString(1,getName());
  return dbStat;
}
