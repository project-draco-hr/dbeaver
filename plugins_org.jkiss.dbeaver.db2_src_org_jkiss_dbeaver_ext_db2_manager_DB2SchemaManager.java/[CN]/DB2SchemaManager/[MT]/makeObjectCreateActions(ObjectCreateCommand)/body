{
  String schemaName=command.getObject().getName();
  SQLDatabasePersistAction action=new SQLDatabasePersistAction("Create schema",String.format(SQL_CREATE_SCHEMA,schemaName));
  return new DBEPersistAction[]{action};
}
