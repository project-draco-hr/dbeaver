{
  final char begin=tok.charAt(0);
  final boolean isIdentifier=Character.isJavaIdentifierStart(begin) || '"' == begin;
  return isIdentifier && !contains(LOGICAL,tok) && !contains(END_CLAUSES,tok)&& !contains(QUANTIFIERS,tok)&& !contains(DML,tok)&& !contains(MISC,tok);
}
