{
  setSorter(new PropertySheetSorter(){
    @Override public int compare(    IPropertySheetEntry entryA,    IPropertySheetEntry entryB){
      return 0;
    }
  }
);
  setPropertySourceProvider(this);
  PropertiesContributor.getInstance().addLazyListener(this);
}
