{
  Runnable runnable=new Runnable(){
    @Override public void run(){
      StandardErrorDialog dialog=new StandardErrorDialog(shell == null ? DBeaverUI.getActiveWorkbenchShell() : shell,title,message,status,IStatus.ERROR);
      dialog.open();
    }
  }
;
  runInUI(shell,runnable);
}
