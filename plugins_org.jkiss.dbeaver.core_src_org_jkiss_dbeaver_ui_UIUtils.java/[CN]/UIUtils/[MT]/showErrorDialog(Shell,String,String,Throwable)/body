{
  if (error != null) {
    log.error(error);
  }
  if (error instanceof DBException) {
  }
  showErrorDialog(shell,title,error == null ? null : message,error == null ? new Status(IStatus.ERROR,DBeaverConstants.PLUGIN_ID,message) : RuntimeUtils.makeExceptionStatus(error));
}
