{
  for (  DBSObject object : objects) {
    if (object instanceof MySQLCatalog) {
      ToolWizardDialog dialog=new ToolWizardDialog(window,new MySQLDatabaseExportWizard((MySQLCatalog)object));
      dialog.open();
    }
  }
}
