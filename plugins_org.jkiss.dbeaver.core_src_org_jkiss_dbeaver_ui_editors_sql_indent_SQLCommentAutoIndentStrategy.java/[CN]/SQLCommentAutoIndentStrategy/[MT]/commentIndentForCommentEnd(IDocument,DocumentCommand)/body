{
  if (c.offset < 2 || d.getLength() == 0) {
    return;
  }
  try {
    if ("* ".equals(d.get(c.offset - 2,2))) {
      c.length++;
      c.offset--;
    }
  }
 catch (  BadLocationException excp) {
  }
}
