{
  if (qualifiers == null) {
synchronized (this) {
      if (qualifiers == null) {
        qualifiers=new ArrayList<WMIQualifier>();
        readObjectQualifiers(qualifiers);
      }
    }
  }
  return qualifiers;
}
