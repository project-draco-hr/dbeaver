{
  if (qualifiers == null) {
synchronized (this) {
      if (qualifiers == null) {
        qualifiers=new ArrayList<>();
        readObjectQualifiers(qualifiers);
      }
    }
  }
  return qualifiers;
}
