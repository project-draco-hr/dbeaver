{
  if (supportedPresentations.isEmpty() && supportedPresentationTypes.isEmpty()) {
    return true;
  }
  return supportedPresentations.contains(presentation.getId()) || supportedPresentationTypes.contains(presentation.getPresentationType());
}
