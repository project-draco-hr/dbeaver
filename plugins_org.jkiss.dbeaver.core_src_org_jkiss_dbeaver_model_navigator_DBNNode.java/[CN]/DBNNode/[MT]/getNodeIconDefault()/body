{
  DBPImage image=getNodeIcon();
  if (image == null) {
    if (this.allowsChildren()) {
      return DBIcon.TREE_FOLDER;
    }
 else {
      return DBIcon.TREE_PAGE;
    }
  }
 else {
    return image;
  }
}
